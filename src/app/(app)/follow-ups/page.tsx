import { CheckSquare } from "lucide-react"
import { PlaceholderView } from "@/components/placeholder-view"

export default function FollowUpsPage() {
  return (
    <PlaceholderView
      icon={CheckSquare}
      title="Follow-ups"
      description="Everything you're waiting on — who, what, and since when. Overdue items surface with quiet urgency so nothing slips."
      phase="Phase 2 — The Connected Graph"
    />
  )
}
