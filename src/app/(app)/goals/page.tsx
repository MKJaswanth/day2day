import { Target } from "lucide-react"
import { PlaceholderView } from "@/components/placeholder-view"

export default function GoalsPage() {
  return (
    <PlaceholderView
      icon={Target}
      title="Goals"
      description="A light layer of goals that projects and learning link to, with a rolled-up view of everything connected to each."
      phase="Phase 2 — The Connected Graph"
    />
  )
}
