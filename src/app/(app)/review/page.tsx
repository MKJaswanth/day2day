import { RefreshCw } from "lucide-react"
import { PlaceholderView } from "@/components/placeholder-view"

export default function ReviewPage() {
  return (
    <PlaceholderView
      icon={RefreshCw}
      title="Weekly Review"
      description="A guided, full-screen ritual — inbox, stalled projects, missing next actions, overdue follow-ups, stalled learning — one decision at a time."
      phase="Phase 4 — The Review Loop"
    />
  )
}
