import { BookOpen } from "lucide-react"
import { PlaceholderView } from "@/components/placeholder-view"

export default function LearningPage() {
  return (
    <PlaceholderView
      icon={BookOpen}
      title="Learning"
      description="A calm shelf of courses, books, topics, and skills — with flexible progress, reflections, and stalled-item surfacing."
      phase="Phase 2 — The Connected Graph"
    />
  )
}
